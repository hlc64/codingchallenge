// create somewhere to store the user input userRecipes
// use a object to store the values entered in. 

// here I would be able to add the recipes. 
//   recipesManager.addRecipe(
//   recipeName.value,
//   incredientsList.value,
//   cookingInstructions.value,
//   cuisine.value,
 
// );

//I would require a empty array for storing the objects
// using the addRecipes to display the recipes on the UI
